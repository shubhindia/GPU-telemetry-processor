apiVersion: v1
kind: Service
metadata:
  name: {{ include "streamer.fullname" . }}
  labels:
    {{- include "streamer.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "streamer.labels" . | nindent 4 }}
  ports:
    - name: identity
      port: 80
      targetPort: 80
